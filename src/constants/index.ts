export enum Scene {
  Preloader = 'preloader-scene',
  Main = 'main-scene',
}

export enum Layer {
  Layer1 = 'Layer 1',
  Layer2 = 'Layer 2',
}

export enum Asset {
  Tileset = 'tileset',
  TilesetName = 'rpg_nature_tileset',
  Map = 'map',
  Items = 'items',
}

export enum Texture {
  Resources = 'resources',
  Player = 'player',
  Enemies = 'enemies',
}

export enum Animation {
  Player = 'player_anim',
  Enemies = 'enemies_anim',
}

export enum Sound {
  Player = 'player',
  Drop = 'drop',
  Bush = 'bush',
  Rock = 'rock',
  Tree = 'tree',
  Bear = 'bear',
  Wolf = 'wolf',
  Ent = 'ent',
}
